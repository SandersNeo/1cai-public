<!--
  n8n Integration Overview
  This document describes the scope, architecture and roadmap for integrating the 1C AI Stack with n8n.
-->
# 1C AI Stack × n8n Integration

> **Status:** MVP ready · **Owner:** AI Platform Team · **Last updated:** 2025-11-06

## 🎯 Зачем это нужно

- Автоматизируем сценарии 1С (code review, миграции, мониторинг) через visual workflows
- Даём безопасный no-code доступ к API 1C AI Stack для аналитиков и DevOps
- Подготавливаем библиотеку готовых сценариев + интеграцию с корпоративными мессенджерами
- Открываем путь к productization (marketplace пакеты, консалтинг)

## 🧩 Архитектура

```
┌─────────────┐      ┌────────────────┐      ┌───────────────────────┐
│  n8n UI /   │─────▶│ OneC AI Node   │─────▶│ 1C AI Stack REST API  │
│  Workflows  │◀─────│ (custom node)  │◀─────│  /api/* endpoints      │
└─────────────┘      └────────────────┘      └───────────────────────┘
        │                       │                        │
        │                       │                        ├─ Neo4j (graph)
        │                       │                        ├─ Qdrant (semantic search)
        │                       │                        └─ PostgreSQL (metadata)
        ▼
  Webhooks · Schedulers · External Integrations
```

## ✅ Требования

- Node.js **≥ 18** (рекомендуется LTS 18/20)
- n8n **≥ 1.50** (self-hosted или Desktop)
- Запущенный 1C AI Stack API:
  - Graph API (`python -m uvicorn src.api.graph_api:app --host 0.0.0.0 --port 8080`)
  - Core API (`python -m uvicorn src.main:app --host 0.0.0.0 --port 8000`)
- Доступ к Postgres/Neo4j/Qdrant согласно .env проекта

## ⚙️ Установка (локально)

```bash
cd integrations/n8n
npm install           # устанавливаем зависимости (типизации n8n + TypeScript)
npm run build         # компилируем в dist/
```

### Подключение к n8n

**Способ 1. Через `N8N_CUSTOM_EXTENSIONS` (рекомендуется для dev):**

```bash
# Linux / macOS
export N8N_CUSTOM_EXTENSIONS="/path/to/1cai-public/integrations/n8n"
n8n start

# Windows (PowerShell)
$env:N8N_CUSTOM_EXTENSIONS = "C:\path\to\repo\integrations\n8n"
n8n start
```

**Способ 2. Локальная установка через npm link:**

```bash
cd integrations/n8n
npm link
cd ~/.n8n/custom    # или в директорию extensions вашего deployment
npm link @onecai/n8n-nodes-onec-ai
```

После рестарта n8n в панеле нод появится раздел **Community → 1C AI Stack**.

## 🔐 Настройка кредов

В n8n создайте кред `1C AI Stack API` (тип `oneCaiApi`):

| Поле | Описание |
|------|----------|
| `Base URL` | Базовый URL вашего API (`http://localhost:8080` для Graph API или `http://localhost:8000` для core API) |
| `API Key` | Bearer токен (если включена авторизация) |
| `Ignore SSL` | Поставьте галочку для self-signed сертификатов (dev окружение) |

> Для разных сервисов (Graph vs Core) удобно завести две записи кредов и выбирать их в workflow.

## 🚀 Поддерживаемые операции

| Resource | Operation | Endpoint | Описание |
|----------|-----------|----------|----------|
| 🔍 `Semantic Search` | 🔎 `search` | `POST /api/search/semantic` | Семантический поиск по базе знаний (Qdrant + embeddings) |
| 🕸️ `Graph` | 🧠 `query` | `POST /api/graph/query` | Произвольный Cypher запрос к Neo4j |
| | 🔗 `dependencies` | `POST /api/graph/dependencies` | Входящие/исходящие вызовы функций 1С |
| | 📚 `configurations` | `GET /api/graph/configurations` | Список конфигураций, загруженных в граф |
| | 🧱 `objects` | `GET /api/graph/objects/{config}` | Объекты конфигурации с фильтрами |
| 🛡️ `Code Review` | 🛠️ `analyze` | `POST /api/code-review/analyze` | Локальный + AI анализ кода (BSP, безопасность, метрики) |
| 🧪 `Test Generation` | 🧪 `generate` | `POST /api/test-generation/generate` | Автоматическая генерация тестов (BSL, TS, Python) |
| 📊 `Statistics` | 📊 `overview` | `GET /api/stats/overview` | Сводные метрики по сервисам (Neo4j/Qdrant/Postgres) |
| | 🩺 `health` | `GET /health` | Проверка здоровья сервиса |
| ⚙️ `Custom Request` | ⚙️ `request` | любой | Произвольный REST вызов c поддержкой body/query/headers |

Все операции поддерживают endpoint override и кастомные заголовки через **Additional Fields**.

## 📦 Примерные workflow'ы

Файлы находятся в `workflows/` и импортируются в n8n через **Import from file**.

| Файл | Сценарий | Что делает |
|------|----------|------------|
| `daily_semantic_digest.json` | Ежедневный дайджест по НДС | Раз в день делает семантический поиск и шлёт email |
| `github_pr_code_review.json` | Code review по Webhook из GitHub | Принимает payload, отправляет на анализ и возвращает JSON с рекомендациями |
| `platform_health_telegram_alert.json` | Мониторинг сервисов | Каждые 10 минут проверяет Neo4j/Qdrant/Postgres и шлёт Telegram-алерт при деградации |

> Workflows используют community-ноду `@onecai/n8n-nodes-onec-ai`. После импорта не забудьте выбрать созданные креды и настроить email/Telegram параметры под вашу инфраструктуру.

## 🧪 Верификация

1. `npm run build` — проверяем, что TypeScript успешно компилируется (dist обновляется автоматически через npm `prepare`)
2. `npm run smoke` — быстрый e2e тест кастомной ноды.
   Перед запуском обязательно:
   1. Соберите пакет (`npm run build`).
   2. Запустите n8n в отдельном окне:

      ```powershell
      cd "C:\path\to\repo\integrations\n8n"
      $env:N8N_CUSTOM_EXTENSIONS = "C:\path\to\repo\integrations\n8n"
      n8n start
      ```

      (если нужен HTTPS и сертификат самоподписанный — добавьте `$env:N8N_IGNORE_SSL = 'true'`)

   3. Во втором окне задайте переменные и запустите smoke-тест:

      ```powershell
      cd "C:\path\to\repo\integrations\n8n"
      $env:N8N_BASE_URL = "http://localhost:5678"     # опционально
      $env:N8N_USER_EMAIL = "user@example.com"
      $env:N8N_USER_PASSWORD = "your-secure-password"
      npm run smoke
      ```

3. В n8n:
   - Импортируйте `workflows/daily_semantic_digest.json`
   - Настройте креды
   - Запустите Cron вручную (кнопка **Execute Node**) и проверьте, что email содержит 5 результатов
4. Для `GitHub PR Code Review` выполните `curl`:

```bash
curl -X POST http://localhost:5678/webhook/onec-ai/pr-review \
  -H "Content-Type: application/json" \
  -d '{"code": "Процедура РассчитатьНДС() ...", "fileName": "Документ.Модуль", "recentChanges": ["..."]}'
```

Получите JSON с рекомендациями и метриками.

## ⚠️ Ограничения / Known Issues

- Авторизация: сейчас поддерживается только Bearer-токен. Для OAuth2 планируется отдельная настройка.
- Rate limiting: используйте Cron/Interval ноды, чтобы не перегружать Qdrant/Neo4j.
- `Code Review` и `Test Generation` могут обращаться к OpenAI (нужны ключи в .env проекта).
- В npm-пакете пока нет собранного `dist` — при деплое в прод публикация в Registry обязательна (`npm publish`).

## 📋 Checklist статуса (обновлено)

- [x] Custom node scaffold + креды
- [x] Реализованы операции (search / graph / code-review / tests / stats / custom)
- [x] TypeScript build (`npm run build`)
- [x] 3 готовых workflow-шаблона
- [x] Quick Start / Installation guide
- [ ] Авто-тесты / интеграция в CI
- [ ] Marketplace публикация и лендинг

## 📅 Следующие шаги

- Подготовить Docker-образ с предустановленными custom extensions (`n8n-custom`)
- Добавить OAuth2 (client credentials) для prod API
- Расширить библиотеку workflow'ов (Jira sync, Confluence doc push, Qdrant reindex)
- Написать статью на Habr / Infostart по интеграции n8n + 1C AI Stack

---

Если нужны дополнительные сценарии — добавляйте JSON в `workflows/` и обновляйте таблицу выше. Contributions welcome!

